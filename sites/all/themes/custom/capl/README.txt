// In order to properly compile the Compass (.scss) files for this project you'll need to do the following:

1. Install ruby gems using the command line. See http://sass-lang.com/install for documentation on other methods of installing and managing ruby gems. Use sudo if permissions issues arise.

  $ bundle install

2. Run compass watch from the command line to poll for changes.

  $ bundle exec compass watch


